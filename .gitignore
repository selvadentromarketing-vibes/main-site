logs
*.log
npm-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.sw?
.env

# Local Netlify folder
.netlify

.claude-flow

# Build artifact of scripts/build-content.mjs (regenerated from content/blog)
src/generated/

# Throwaway analysis scripts and image dumps (agents and humans both leave
# these in the repo root). Never part of the site — public/ is the only
# directory copied into dist/.
scratch-*
